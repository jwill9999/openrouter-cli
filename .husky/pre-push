#!/usr/bin/env sh
# Run tests and build before pushing (skip in CI)
if [ -n "$CI" ] || [ "$HUSKY" = "0" ] || [ -n "$HUSKY_SKIP_HOOKS" ]; then
  exit 0
fi

npm run lint --fix && npm run format:check && npm test && npm run build
