#!/usr/bin/env sh
# Validate commit messages follow Conventional Commits (skip in CI)
if [ -n "$CI" ] || [ "$HUSKY" = "0" ] || [ -n "$HUSKY_SKIP_HOOKS" ]; then
  exit 0
fi
npx --no -- commitlint --edit "$1"
